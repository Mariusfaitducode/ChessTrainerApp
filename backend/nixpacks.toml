# Configuration Nixpacks pour Railway
# Installe Stockfish et configure l'environnement Python

[phases.setup]
nixPkgs = ["stockfish"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"

