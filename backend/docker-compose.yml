version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - STOCKFISH_PATH=/usr/games/stockfish
      - MAX_DEPTH=25
      - DEFAULT_DEPTH=15
      - CORS_ORIGINS=*
    restart: unless-stopped
